% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code.R
\name{ggRibo_tx}
\alias{ggRibo_tx}
\title{Plot RNA-seq and Ribo-seq Coverage in Transcript Coordinates}
\usage{
ggRibo_tx(
  gene_id = NULL,
  tx_id = NULL,
  eORF.tx_id = NULL,
  eORFRangeInfo = NULL,
  Extend = 100,
  NAME = "",
  RNAcoverline = "grey",
  RNAbackground = "#FEFEAE",
  fExtend = 0,
  tExtend = 0,
  RNAseq = inputs_full$RNAseq,
  Riboseq = inputs_full$Riboseq,
  SampleNames = Samples,
  GRangeInfo = Txome_Range,
  RNAseqBamPaired = RNAseqBamPairorSingle,
  Y_scale = "all",
  Ribo_fix_height = NULL,
  RNA_fix_height = NULL,
  plot_ORF_ranges = TRUE,
  frame_colors = c(`0` = "#FF0000", `1` = "#3366FF", `2` = "#009900"),
  sample_color = rep("color", length(Riboseq)),
  show_seq = FALSE,
  FASTA = NULL,
  dna_aa_height_ratio = 0.5,
  gene_model_height_ratio = 1.5,
  transcript_label_font_size = 10,
  gene_model_coord_font_size = 8.8,
  plot_genomic_direction = FALSE,
  data_types = rep("Ribo-seq", length(SampleNames)),
  plot_range = NULL,
  nucleotide_color_scheme = "default",
  oORF_coloring = "extend_mORF",
  ribo_linewidth = 0.5,
  rna_linewidth = 0.5,
  axis_label_font_size = 11,
  axis_title_font_size = 10,
  sample_label_font_size = 3,
  title_font_size = 5
)
}
\arguments{
\item{gene_id}{Character string specifying the gene ID of interest.}

\item{tx_id}{Character string specifying the transcript ID to plot.}

\item{eORF.tx_id}{Optional. Vector of eORF transcript IDs to include in the plot.}

\item{eORFRangeInfo}{Optional. An object (e.g., \code{eORF_Range}) providing eORF ranges. If \code{NULL}, eORFs won't be plotted.}

\item{Extend}{Number of bases to extend the plotting region beyond the transcript. Default is \code{100}.}

\item{NAME}{Optional. Additional name or title for the plot.}

\item{RNAcoverline}{Color for the RNA-seq coverage step line. Default is \code{"grey"}.}

\item{RNAbackground}{Fill color for RNA-seq coverage bars. Default is \code{"#FEFEAE"}.}

\item{fExtend}{Number of nucleotides to extend into 5' UTR for frame assignment. Default is \code{0}.}

\item{tExtend}{Number of nucleotides to extend into 3' UTR for frame assignment. Default is \code{0}.}

\item{RNAseq}{A list describing RNA-seq input(s). Each entry can be a file path or coverage descriptor.}

\item{Riboseq}{A list describing Ribo-seq input(s). Each entry can be a file path or coverage descriptor.}

\item{SampleNames}{Vector of sample names. Must match \code{RNAseq} and/or \code{Riboseq} length.}

\item{GRangeInfo}{A \code{Txome_Range}-like object containing genome/transcript annotations.}

\item{RNAseqBamPaired}{Vector indicating pairing for BAM (e.g. \code{"paired"} or \code{"single"}). Not used for bigWig.}

\item{Y_scale}{Either \code{"all"} or \code{"each"}, controlling y-scaling across samples. Default is \code{"all"}.}

\item{Ribo_fix_height}{Numeric to fix the max Ribo-seq coverage height. Default is \code{NULL}.}

\item{RNA_fix_height}{Numeric to fix the max RNA-seq coverage height. Default is \code{NULL}.}

\item{plot_ORF_ranges}{Logical; if \code{TRUE}, attempt to plot eORFs in the gene model. Default is \code{TRUE}.}

\item{frame_colors}{Named vector of colors for reading frames 0,1,2.}

\item{sample_color}{Either \code{"color"} or a vector of colors for each sample, controlling how Ribo-seq reads are drawn.}

\item{show_seq}{Logical; if \code{TRUE}, also plot the transcript DNA/AA via \code{plotDNAandAA_tx}. Default is \code{FALSE}.}

\item{FASTA}{A \code{BSgenome} object with the reference sequences. Needed if \code{show_seq=TRUE}.}

\item{dna_aa_height_ratio}{Numeric adjusting the vertical space for DNA/AA. Default is \code{0.5}.}

\item{gene_model_height_ratio}{Numeric adjusting the height of the transcript model. Default is \code{1.3}.}

\item{transcript_label_font_size}{Numeric controlling transcript label font size in the gene model. Default is \code{10}.}

\item{gene_model_coord_font_size}{Numeric font size for X-axis genomic coordinate labels in the gene model panel. Default is 8.8.}

\item{plot_genomic_direction}{If \code{TRUE}, attempts to draw an arrow for the genomic direction in coverage plots.}

\item{data_types}{Vector describing the type of each sample (e.g. \code{"Ribo-seq"} or \code{"RNA-seq"}). Must match \code{SampleNames} length.}

\item{plot_range}{Optional numeric \code{c(start,end)} specifying the transcript coordinate range to plot.}

\item{nucleotide_color_scheme}{If \code{"colorblind"}, uses a color-blind-friendly palette for nucleotides in DNA/AA. Otherwise uses \code{"default"}.}

\item{oORF_coloring}{Coloring scheme for overlapping ORFs: \code{"extend_mORF"} (use main CDS frame for overlapping eORFs) or \code{"oORF_colors"} (use eORF-specific frames). Default is \code{"extend_mORF"}.}

\item{ribo_linewidth}{Numeric value to control the thickness of Ribo-seq read count lines. Default is \code{0.5}.}

\item{rna_linewidth}{Numeric value to control the thickness of RNA-seq step lines. Default is \code{0.5}.}

\item{axis_label_font_size}{Numeric controlling axis tick label size in coverage panels. Default is 11.}

\item{axis_title_font_size}{Numeric controlling axis title size (e.g., y-axis titles). Default is 10.}

\item{sample_label_font_size}{Numeric controlling the size of sample name annotations in panels. Default is 3.}

\item{title_font_size}{Numeric controlling the size of the top title text. Default is 5.}
}
\value{
A combined \code{ggplot} object displaying RNA-seq coverage, Ribo-seq coverage, optional eORFs, a transcript model,
and (if requested) the spliced DNA/AA sequences, all in transcript coordinates.
}
\description{
This function plots RNA-seq coverage and frame-specific Ribo-seq read counts
for a specified transcript in \emph{transcript} coordinates (exon-spliced).
It optionally plots eORFs, genomic sequences, and a transcript model.
}
